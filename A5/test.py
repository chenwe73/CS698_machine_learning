import numpy as np
import matplotlib.pyplot as plt
from PIL import Image


A = np.ones((2,10)
B = np.zeros((8,10)
C = np.concatenate((A,B), axis = 0)
print C



for d in range(2):
	for t in range(1,9):
		if (d == 0)
			sum(C[0:t,:]) / C[0:t,:].size
		else:
			sum(C[:,0:t]) / C[:,0:t].size

